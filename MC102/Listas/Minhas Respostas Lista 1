1
	a - int
	b - double
	c - float
	d - float

2
	Na primeira atribuição dizemos que a variável ‘a’ é um caracter apenas, e portanto só pode armazenar um dígito.
	Na segunda atribuição damos á variável ‘a’ o dígito 6, enquanto que na terceira atribuição damos á variável o dígito que é representado por 6~na tabela ASCII.

3
	#include <stdio.h>
	#include <math.h>

	int main () {

		double x, y;

		scanf("%lf", &x);

		y = sqrt(x) + (x/2) + pow(x , x);

		printf("%lf\n", y);

		return 0;

	}

4
	#include <stdio.h>
	#include <math.h>

	int main () {

		int x, y;

		printf("Coloque um valor para x\n");

		scanf("%d", &x); -3

		printf("Coloque um valor para y\n");

		scanf("%d", &y); 4

		x = y - x;

		y = y - x;

		x = x + y;

		printf("invertendo temos que x é %d e y é %d\n", x, y );

	}

5
	#include <stdio.h>
	#include <math.h>

	int main ()
	{
		int x, y, z, s;
		float A;

		printf("Coloque um valor para x\n");

		scanf("%d", &x);

		printf("Coloque um valor para y\n");

		scanf("%d", &y);

		printf("Coloque um valor para z\n");

		scanf("%d", &z);

		if ( x == y && x== z)
		{
			printf("O triangulo é equilátero\n");
		}

		else if ( (x == y && x != z) || (x == z && x != y) || ( z == y && z != x) )
		{
			printf("O triangulo é isóceles\n");
		}

		else if ( x != y && y != z && x != z)
		{
			printf("O triangulo é escaleno\n");
		}

		s = (x + y + z)/2;
		A = sqrt(s*(s - x)*(s - y)*(s - z));

		printf("A área é de %f\n", A);
	}

6
	Há um erro no código pois ele printa "O número é par e maior ou igual a 100\n" se não atender ao if ( ( a % 2 == 0) && (a<100) ), o que está errado pois isso quer dizer que o número ou é maior que 100 ou não é par (ou não é ambos), mas não quer dizer que seja maior que 100 e par, pois pode vir a ser ímpar.
	Um erro similar ocorre com o programa na segunda parte, a de teste de ser ímpar.

7
	#include <stdio.h>

	int main()
	{
		int a, b, c;

		scanf("%d", &a);

		scanf("%d", &b);

		scanf("%d", &c);

		if ( a >= b && b >= c)
		{
		 	printf("%d\n%d\n%d\n", a,b,c);
		}

		else if ( a >= c && c > b)
		{
			printf("%d\n%d\n%d\n", a,c,b);
		}

		else if ( b >= a && a > c)
		{
			printf("%d\n%d\n%d\n", b,a,c);
		}

		else if ( b > c && c > a)
		{
			printf("%d\n%d\n%d\n", b,c,a);
		}

		else if ( c > a && a > b)
		{
			printf("%d\n%d\n%d\n", c,a,b);
		}

		else if ( c > b && b > a)
		{
			printf("%d\n%d\n%d\n", c,b,a);
		}
	}